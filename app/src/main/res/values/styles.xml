<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionMenuTextColor">#000000</item>
        <item name="toolbarStyle">@style/Toolbar</item>
        <item name="android:textAlignment">gravity</item>
        <item name="android:textDirection">locale</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppThemeDark" parent="AppTheme">
        <item name="android:statusBarColor">#000000</item>
        <item name="toolbarStyle">@style/ToolbarDark</item>
    </style>

    <style name="AppThemeDark.NoActionBar" parent="AppThemeDark">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppThemeDayNight.Fullscreen" parent="AppThemeDayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Colour the toolbar correctly. -->
    <style name="Toolbar">
        <item name="android:minHeight">?actionBarSize</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextAppearance">@style/TextAppearance.AppCompat.Title</item>
        <item name="titleMarginStart">16dp</item>
    </style>

    <style name="ToolbarDark" parent="Toolbar">
        <item name="android:background">@color/colorPrimaryNight</item>
    </style>


    <!-- Settings screen theme. -->
    <style name="SettingsTheme" parent="AppTheme">
        <item name="preferenceTheme">@style/WidePreferenceThemeOverlay</item>
    </style>

    <style name="SettingsThemeDark" parent="AppThemeDark">
        <item name="preferenceTheme">@style/WidePreferenceThemeOverlay</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="WidePreferenceThemeOverlay" parent="PreferenceThemeOverlay">
        <item name="preferenceFragmentListStyle">@style/WidePreferenceFragmentListStyle</item>
    </style>

    <style name="WidePreferenceFragmentListStyle" parent="PreferenceFragmentList">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
    </style>
</resources>
